@model IEnumerable<TuHotelEnLinea.Models.CustomerXRoom>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Customer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Room)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CustomerCreatedAt)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.CustomerIdCard)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Room.RoomId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerCreatedAt)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CustomerXRoomId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CustomerXRoomId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CustomerXRoomId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
