@model IEnumerable<TuHotelEnLinea.Models.CategoryRoom>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategoryRoomName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CategoryRoomDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryRoomName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryRoomDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CategoryRoomId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CategoryRoomId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CategoryRoomId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
